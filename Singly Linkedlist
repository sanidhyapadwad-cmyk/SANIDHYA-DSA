#include <iostream>
using namespace std;
    struct node{
        int data;
        node*link;
        
    };
    int main(){
        int val,n,i;
        node*first,*temp,*last;
        cout<<"Enter the size of list:";
        cin>>n;
        first=new node;
        cout<<"Enter the value to be storade in node1:";
        cin>>first->data;
        first->link=NULL;
        temp=first;
        for(i=1;i<n;i++)
        {
            last=new node;
            cout<<"Enter the value to be storade in node"<<i+1<<":";
            cin>>last->data;
            last->link=NULL;
           temp->link=last;
            temp=last;
        } 
        cout<<"The linked list is :";
        
      {temp=first;
        while(temp!=NULL)
        {
            cout<<temp->data<<" ";
        temp=temp->link;
        }
    
      }
      bool flag=0;
      int key;
      cout<<"Enter the value to be searched :";
      cin>>key;
      if(n==key)
    
        return 0;
    }
        
    
   
